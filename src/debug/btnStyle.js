// Shared by every button in the debug panel and its lift-ride scrub, so the
// two read as one control surface rather than two hand-matched ones.
export const btnStyle = {
  fontSize: 10,
  padding: '4px 8px',
  borderRadius: 4,
  border: '1px solid rgba(255,255,255,0.25)',
  background: 'transparent',
  color: '#fff',
  cursor: 'pointer',
};
