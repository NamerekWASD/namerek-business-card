// ── 2. UG, the left half — deliberately empty ────────────────────────────────
// This deck has been three things. It began as a PROJEKTE heading, four
// statistics badges and a plate reading "Referenzen" that said projects were in
// preparation while the console beside it was already showing them. NBC-22
// replaced all of that with a works notice: one enamel plate screwed to the
// plaster, carrying the description of whatever job is on the glass, as real
// selectable DOM text — which is what `project_deck_text_stays_dom` argued for
// and why 3D text was turned down at the time.
//
// NBC-68 turned it down the other way, and Mykolai's reason is better than the
// one it overruled. The plate had one job — say what the project *is* — and it
// was failing at it in a way no amount of typography fixes: when the console
// paged into a different job, the paragraph quietly swapped its words and
// nobody saw it happen. "Сложно понять что описание изменилось." A plate cannot
// go dark and come back; a tube can, and that is the whole of the fix. On the
// question of selecting the text: "человеку его незачем выделять" — it is one
// sentence next to a screen already showing the thing and a plate already
// naming it, and nobody has ever wanted it on their clipboard.
//
// So the notice is `NoticeScreen`, in the WebGL half of this same landing, and
// what is left here is nothing. Deliberately nothing rather than deleted: the
// column's own layout, the door-closure gate and `DECK_BODIES` all expect a
// component for every floor, and a floor whose text lives in the scene is a
// fact worth stating in the file someone will come looking in.
//
// ── one surface, one question ────────────────────────────────────────────────
// Unchanged, and the reason there is nothing to put back here: the glass shows
// what it looks like, the console plate says which job it is, the screen on the
// wall says what it is and why, and the box on the belt says what shipped. The
// table is in the header of `projects.js`.

function ProjekteDeck() {
  return null;
}

export default ProjekteDeck;
